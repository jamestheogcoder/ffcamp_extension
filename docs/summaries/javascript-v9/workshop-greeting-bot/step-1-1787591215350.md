# Build a Greeting Bot: Step 1 | freeCodeCamp.org

> Source: https://www.freecodecamp.org/learn/javascript-v9/workshop-greeting-bot/step-1 · Saved 8/24/2026, 10:51:55 PM

## Original Content

#### Step 1

In this workshop you will learn how to work with JavaScript fundamentals by building a greeting bot.

In this first step, you will want to output a message to the console from the greeting bot.

Remember that you learned about console.log() and strings in previous lessons.

Here is a reminder of how to use console.log() with strings:

console.log("Hello, World!");

Add a console.log() statement that outputs the string "Hi there!" to the console. Don't forget your quotes around the message!

/** * Your test output will go here */

<!--FFCAMP-SPLIT-->

## What the topic is explaining
## What the MCQs are asking
## What it means
## Most important things to know

No preamble. Concise bullets. There are no MCQs on page? It says any MCQs on it. If none, say no MCQs? Need section still. Could say "There are no MCQs in the provided content." But maybe need explain what MCQs are asking. Since none, state none. Ensure exact headings.

Content: Build Greeting Bot Step 1. Learn JS fundamentals by building greeting bot. Output message to console. console.log and strings. Add console.log("Hi there!"). Don't forget quotes.

Need simple terms.

Potential final:
## What the topic is explaining
- This step introduces a small JavaScript project: a greeting bot.
- It focuses on printing a text message to the browser console using `console.log()`.
- It reminds that text values are strings and must be wrapped in quotes.

## What the MCQs are asking
- There are no MCQs in the provided page content.
- The task is a coding exercise: write a `console.log()` statement that prints exactly `"Hi there!"`.

## What it means
- The bot “greets” the user by displaying a message in the console.
- `console.log()` is a debugging/output tool, not a visible webpage message.
- The string `"Hi there!"` is the exact text the program should output.

## Most important things to know
- Use `console.log()` to send output to the console.
- Put the message inside quotes: `console.log("Hi there!");`
- The quotes are required because the message is a string.
- The expected output is exactly `Hi there!` without extra text.

Need ensure no extra. Good.
</think>

## What the topic is explaining
- This step introduces a small JavaScript project: a greeting bot.
- It focuses on printing a text message to the console using `console.log()`.
- It reminds that text values are strings and must be wrapped in quotes.

## What the MCQs are asking
- There are no MCQs in the provided page content.
- The task is a coding exercise: write a `console.log()` statement that prints exactly `"Hi there!"`.

## What it means
- The bot “greets” the user by displaying a message in the console.
- `console.log()` is a debugging/output tool, not a visible webpage message.
- The string `"Hi there!"` is the exact text the program should output.

## Most important things to know
- Use `console.log()` to send output to the console.
- Put the message inside quotes: `console.log("Hi there!");`
- The quotes are required because the message is a string.
- The expected output is exactly `Hi there!` without extra text.
