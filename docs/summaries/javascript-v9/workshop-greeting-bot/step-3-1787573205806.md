# Build a Greeting Bot: Step 3 | freeCodeCamp.org

> Source: https://www.freecodecamp.org/learn/javascript-v9/workshop-greeting-bot/step-3 · Saved 8/24/2026, 5:51:45 PM

## Original Content

#### Step 3

In previous lessons, you learned about the let keyword and how to declare variables in JavaScript.

Here is a reminder of how to declare a variable using the let keyword:

let greeting;

Use the let keyword to declare a variable called bot.

NOTE: You are using let here because later on in the workshop, you will be changing the value of the bot variable.

Hi there! I am excited to talk to you.

<!--FFCAMP-SPLIT-->

## What the topic is explaining
- Review of variable declaration with `let` in JavaScript
- How to declare a variable: `let greeting;`
- Task to declare a variable named `bot` using `let`
- Reason for using `let` is future reassignment in the workshop

## What the MCQs are asking
- No MCQs are included in the provided page content for Step 3

## What it means
- `let` creates a mutable variable that can be reassigned later
- Declaring `bot` now sets up a placeholder to be updated as the greeting bot is built
- This step is about syntax practice, not functionality yet

## Most important things to know
- Use `let bot;` exactly, not `const` or `var`
- `let` allows the value to change later; `const` would not
- Declaration is separate from assignment; value can be set in a later step
