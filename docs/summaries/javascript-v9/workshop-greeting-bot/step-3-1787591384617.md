# Build a Greeting Bot: Step 3 | freeCodeCamp.org

> Source: https://www.freecodecamp.org/learn/javascript-v9/workshop-greeting-bot/step-3 · Saved 8/24/2026, 10:54:44 PM

## Original Content

#### Step 3

In previous lessons, you learned about the let keyword and how to declare variables in JavaScript.

Here is a reminder of how to declare a variable using the let keyword:

let greeting;

Use the let keyword to declare a variable called bot.

NOTE: You are using let here because later on in the workshop, you will be changing the value of the bot variable.

/** * Your test output will go here */

<!--FFCAMP-SPLIT-->

## What the topic is explaining
- This step introduces declaring a JavaScript variable using the `let` keyword.
- It reminds you that `let` is used to create a variable that can be changed later.
- The specific task is to declare a variable named `bot`.

## What the MCQs are asking
- There are no MCQs shown on this page.
- The exercise is a coding task: write the code to declare `bot` using `let`.

## What it means
- You are creating a variable called `bot` that can store a value later.
- Because `let` is used, the value of `bot` can be updated in later steps.
- This is likely part of building a greeting bot, where `bot` may hold a message or state.

## Most important things to know
- Use the exact syntax: `let bot;`
- `let` declares a variable that can be reassigned.
- The variable name must be `bot`.
- No value is assigned yet; the variable is just declared.
