# Build a Greeting Bot: Step 3 | freeCodeCamp.org

> Source: https://www.freecodecamp.org/learn/javascript-v9/workshop-greeting-bot/step-3 · Saved 8/24/2026, 5:51:11 PM

## Original Content

#### Step 3

In previous lessons, you learned about the let keyword and how to declare variables in JavaScript.

Here is a reminder of how to declare a variable using the let keyword:

let greeting;

Use the let keyword to declare a variable called bot.

NOTE: You are using let here because later on in the workshop, you will be changing the value of the bot variable.

Hi there! I am excited to talk to you.

<!--FFCAMP-SPLIT-->

## What the topic is explaining
- Review of variable declaration with `let` in JavaScript
- How to declare a variable named `bot` using `let`
- Why `let` is chosen over `const` here: the value will be reassigned later in the workshop

## What the MCQs are asking
- No MCQs are included in the provided page content for Step 3

## What it means
- `let greeting;` is the syntax pattern to follow for declaring a variable without initializing it
- Declaring `bot` with `let` makes it mutable so future steps can change its value
- This is a setup step for building a greeting bot, not the bot logic itself

## Most important things to know
- `let` allows reassignment; `const` would not
- Declaration syntax is `let variableName;`
- The variable `bot` is intentionally left uninitialized for now
