# Build a Greeting Bot: Step 2 | freeCodeCamp.org

> Source: https://www.freecodecamp.org/learn/javascript-v9/workshop-greeting-bot/step-2 · Saved 8/25/2026, 11:20:08 AM

## Original Content

#### Step 2

Now you should see the first message from the bot in the console.

It is time to add a second message from the bot.

Add another console.log statement to output the message "I am excited to talk to you." to the console.

Hi there!

<!--FFCAMP-SPLIT-->

## What the topic is explaining
- This step continues a simple JavaScript greeting bot.
- It asks you to print a second message to the console using console.log.
- The required message is "I am excited to talk to you."
- The first message, "Hi there!", should already be visible from the previous step.

## What the MCQs are asking
- No multiple-choice questions are included in the provided content.
- The task is a hands-on coding instruction rather than a quiz.

## What it means
- You are learning to use console.log to send text output to the browser console.
- Each console.log statement creates a separate line/message.
- Adding another statement shows how a program can produce multiple outputs in sequence.

## Most important things to know
- Use console.log("I am excited to talk to you.");
- Keep the exact wording and punctuation required.
- The bot now sends two messages: "Hi there!" and "I am excited to talk to you."
- This is a basic example of sequential execution in JavaScript.

Need ensure no extra after last. Good.
</think>

## What the topic is explaining
- This step continues a simple JavaScript greeting bot.
- It asks you to print a second message to the console using `console.log`.
- The required message is `"I am excited to talk to you."`
- The first message, `"Hi there!"`, should already be visible from the previous step.

## What the MCQs are asking
- No multiple-choice questions are included in the provided content.
- The task is a hands-on coding instruction rather than a quiz.

## What it means
- You are learning to use `console.log` to send text output to the browser console.
- Each `console.log` statement creates a separate line/message.
- Adding another statement shows how a program can produce multiple outputs in sequence.

## Most important things to know
- Use `console.log("I am excited to talk to you.");`
- Keep the exact wording and punctuation required.
- The bot now sends two messages: `"Hi there!"` and `"I am excited to talk to you."`
- This is a basic example of sequential execution in JavaScript.
