# Build a Greeting Bot: Step 2 | freeCodeCamp.org

> Source: https://www.freecodecamp.org/learn/javascript-v9/workshop-greeting-bot/step-2 · Saved 8/24/2026, 9:49:48 PM

## Original Content

#### Step 2

Now you should see the first message from the bot in the console.

It is time to add a second message from the bot.

Add another console.log statement to output the message "I am excited to talk to you." to the console.

/** * Your test output will go here */

<!--FFCAMP-SPLIT-->

## What the topic is explaining
## What the MCQs are asking
## What it means
## Most important things to know
Use concise bullets. Explain concepts in simple terms and connect questions to underlying ideas. No preamble before first heading, nothing after last. Start directly with heading.

Need analyze content. It's a freeCodeCamp JavaScript v9 workshop greeting bot step 2. Content: Now you should see first message from bot in console. Time to add second message. Add another console.log statement to output message "I am excited to talk to you." to console. There are no MCQs on page. Need section "What the MCQs are asking" likely say no MCQs present, but if any, they'd ask to add console.log with exact string. Need be careful: exactly four sections. Bullets concise.

Need final only Markdown. Ensure no extra. Could include:
## What the topic is explaining
- This step teaches how to print multiple messages to the browser console using `console.log()`.
- It builds on the first message by adding a second bot message.
- The goal is to output the exact text: `I am excited to talk to you.`

## What the MCQs are asking
- There are no multiple-choice questions on this page.
- The task is a hands-on exercise: add another `console.log` statement with the required message.

## What it means
- `console.log()` sends text or values to the console for debugging or display.
- Each `console.log()` call can print one line/message.
- To show multiple bot messages, use multiple `console.log()` statements.

## Most important things to know
- Use the exact string: `"I am excited to talk to you."`
- Include the period inside the quotes.
- Example: `console.log("I am excited to talk to you.");`
- The console output should show both the first message and this new message.

Need ensure no MCQs? The instruction says Given page content (and any MCQs on it). If none, we can state none. Good.

Double-check exact headings. No preamble. Final.
</think>

## What the topic is explaining
- This step teaches how to print multiple messages to the browser console using `console.log()`.
- It builds on the first bot message by adding a second message.
- The goal is to output the exact text: `I am excited to talk to you.`

## What the MCQs are asking
- There are no multiple-choice questions on this page.
- The task is a hands-on exercise: add another `console.log` statement with the required message.

## What it means
- `console.log()` sends text or values to the console for display or debugging.
- Each `console.log()` call can print one message.
- To show multiple bot messages, use multiple `console.log()` statements.

## Most important things to know
- Use the exact string: `"I am excited to talk to you."`
- Include the period inside the quotes.
- Example: `console.log("I am excited to talk to you.");`
- The console should show both the first message and this new message.
