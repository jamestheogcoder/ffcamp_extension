# Build a Greeting Bot: Step 3 | freeCodeCamp.org

> Source: https://www.freecodecamp.org/learn/javascript-v9/workshop-greeting-bot/step-3 · Saved 8/24/2026, 6:57:10 PM

## Original Content

#### Step 3

In previous lessons, you learned about the let keyword and how to declare variables in JavaScript.

Here is a reminder of how to declare a variable using the let keyword:

let greeting;

Use the let keyword to declare a variable called bot.

NOTE: You are using let here because later on in the workshop, you will be changing the value of the bot variable.

Hi there! I am excited to talk to you.

<!--FFCAMP-SPLIT-->

## What the topic is explaining
- Review of variable declaration with `let` in JavaScript
- How to declare a variable named `bot` using `let`
- Why `let` is chosen over `const` when a value will be reassigned later in the workshop

## What the MCQs are asking
- No MCQs are provided in the page content for this step

## What it means
- `let greeting;` is the syntax pattern to declare a variable without assigning a value yet
- `let bot;` will create a mutable variable that can be assigned and changed later as the greeting bot is built
- Using `let` signals intent to reassign, while `const` would prevent reassignment

## Most important things to know
- `let` declares a block-scoped mutable variable
- Declaration can be separate from initialization
- Choose `let` when the value will change later; choose `const` when it will not
