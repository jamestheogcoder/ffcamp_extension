# Build a Greeting Bot: Step 4 | freeCodeCamp.org

> Source: https://www.freecodecamp.org/learn/javascript-v9/workshop-greeting-bot/step-4 · Saved 8/24/2026, 8:38:18 PM

## Original Content

#### Step 4

Now, it is time to assign a value to your bot variable.

In previous lessons, you learned how to assign values to variables like this:

variableName = "Here is the value";

Remember that what is on the right side of the assignment operator = is the value that you are assigning to the variable on the left side.

Assign the string "teacherBot" to the bot variable.

SyntaxError: unknown: Unterminated string constant. (2:12) 1 | console.log("Hi there!"); > 2 | console.log("I | ^

<!--FFCAMP-SPLIT-->

## What the topic is explaining
- How to assign a value to a variable using `variableName = value`
- The right side of `=` is the value, left side is the variable name
- The task is to assign the string `"teacherBot"` to the variable `bot`
- Shows a common syntax error: Unterminated string constant from a missing closing quote

## What the MCQs are asking
- No MCQs are included in the provided page content

## What it means
- `bot = "teacherBot"` stores the text teacherBot in the variable bot for later use
- Strings in JavaScript must be wrapped in matching quotes `"` or `'`
- An unterminated string error means the parser reached a new line before finding the closing quote

## Most important things to know
- Assignment syntax is `variable = value`
- String literals need opening and closing quotes
- `bot = "teacherBot"` is the required assignment for this step
