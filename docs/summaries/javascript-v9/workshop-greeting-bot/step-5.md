# Build a Greeting Bot: Step 5 | freeCodeCamp.org

> Source: https://www.freecodecamp.org/learn/javascript-v9/workshop-greeting-bot/step-5 · Saved 8/24/2026, 8:47:44 PM

## Original Content

#### Step 5

Now, it is time to initialize the botLocation variable.

When you need to declare variables with multiple words, you can use the camelCase naming convention.

When using camelCase, the first word is all lowercase and the first letter of each subsequent word is capitalized.

Here is an example:

let thisIsCamelCase = "Alice the camel is actually a horse.";

Declare and assign the string "the universe" to the botLocation variable on the same line using the let keyword.

/** * Your test output will go here */

<!--FFCAMP-SPLIT-->

## What the topic is explaining
- How to declare and initialize a variable in JavaScript with `let`
- Using camelCase for multi-word variable names: first word lowercase, subsequent words capitalized
- The specific task is to create `botLocation` and assign it the string `"the universe"`

## What the MCQs are asking
- No MCQs are included in the provided page content for Step 5

## What it means
- `let botLocation = "the universe";` creates a mutable variable named `botLocation`
- The name follows camelCase convention, which is the standard JavaScript naming style for variables
- The value stored is the literal string "the universe", which will be used later by the greeting bot

## Most important things to know
- `let` declares a block-scoped variable that can be reassigned
- Variable names must be camelCase, not snake_case or spaces
- Declaration and assignment can happen on the same line: `let botLocation = "the universe";`
